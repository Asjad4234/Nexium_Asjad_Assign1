'use client';

import QuoteGenerator from "@/components/QuoteGenerator";

export default function Home() {
  return (
    <main>
      <QuoteGenerator />
    </main>
  );
}
// This is the main page of the application where the QuoteGenerator component is rendered.
// The QuoteGenerator component is responsible for displaying and managing the quotes.
// It is imported from the components directory and used within the main function.